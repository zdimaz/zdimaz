# Install

```bash
# Clone the repository
git clone https://github.com/zdimaz/zdimaz.git
cd zdimaz

# No build step required – the profile is static.
# Open the README locally to view the hero banner:
code README.md
```

All assets are SVG‑only, so they render correctly on GitHub without additional tooling.
